# 自動作業の実行計画（第2弾：2026-08-17 午後〜）

**中断・再開の起点はこのファイル。** 第1弾（8/16深夜〜8/17朝）の記録は git 履歴を参照。

## 再開する人（＝次のセッション）へ

1. `docs/tasks/.autonomous-lock` の更新時刻を確認。**60分以内なら別セッションが作業中**なので何もせず終了。
2. 下の作業一覧で `[ ]` の最も上の項目を1つ選ぶ。
3. 実装 → `npx tsc --noEmit` / `npm run lint` / `npm run test` / `npm run build` を通す → コミット →
   このファイルを `[x]` に更新 → `.autonomous-lock` を現在時刻で更新。
4. 作業ブランチ: **`main` に直接コミットしてよい**（前回のブランチは既にマージ済み）。
   ただし **push は明示的な指示があるときだけ**。本番へ即反映されるため。
5. すべて `[x]` になったら PROJECT_STATUS.md を更新し、スケジュールタスクを削除して終了。

### 守ること

- DBマイグレーションは自動適用されない。SQLを書いたら NIGHT_BATCH_HANDOVER.md に追記する。
- 本番DBへの書き込みはしない（読み取りは可）。
- force push・履歴の書き換えをしない。

---

## 発注者の指示（2026-08-17 昼）

### 管理画面

- ライブ編集（画面を見ながら直す）の**修正できる範囲を最大限広げる**。
  **自然言語（日本語）の部分はすべてクリックで編集できる**レベルにする。
- リンクは「**リンクに飛ぶ / 編集する**」を選べるようにし、**ボタンの文言も変えられる**ようにする。
- **「ジャガーを知る」のトップページも編集可能**にする。
- ライブ編集に「**画像を挿入する**」という選択肢と、**高い自由度のある操作仕様**を追加する。
  サイト全体で写真を増やしたい。

### 公開サイト

- 「ジャガーを知る」が**初期の情報に偏っている**（在庫が多い年代ではない）。
  初期〜1980年代はまとめつつ、**1990年代〜2015年頃**を整理する。特に **XJ / XK / Xタイプ**。
- 当時の**英国車の素晴らしさ・上質さ**、**オーナーとして選ぶ人の特徴**、
  **どんな方に選ばれてきたか**が伝わる内容にする。
- **エンジンの素晴らしさ・設計思想・鋳造エンジンブロックの堅牢さ**など、
  安心につながる情報を入れる。

---

## 作業一覧

### ラウンドA: 監査で見つかった不具合の修正（本番に出ているもの優先）

- [x] A-1 `Editable` が編集モードOFFのとき className を捨てる（車両詳細の写真枠の余白が消えている）
- [x] A-2 管理ダッシュボードの「整備実績を追加する」が404を指す
- [x] A-3 公開サイトから `/library` `/encyclopedia` 一覧への入口が無い（フッターに置く）
- [x] A-4 関連車両カードで、slug無し車両を除外した後の添字ズレで別の車の写真が出る
- [x] A-5 `resolveEditableField` がプロトタイプ由来のキー（constructor等）を通す
- [ ] A-6 本文を空にすると公開画面から消え、ライブ編集で二度と書き戻せない
- [x] A-7 NOT NULL の本文列を空にすると原因不明の500になる
- [ ] A-8 引き継ぎ資料の「まだやっていないこと」が実装済みの項目を未実装と書いている

### ラウンドB: ライブ編集の拡張（発注者の最優先）

- [ ] B-1 編集できる範囲を「画面上の日本語すべて」に広げる（見出し・ボタン・説明文・空欄も）
- [x] B-2 リンクを押したとき「リンクに飛ぶ / 編集する」を選べるようにする
- [ ] B-3 ボタン・リンクの文言を編集できるようにする
- [x] B-4 「ジャガーを知る」(/jaguar) を編集可能にする
- [x] B-5 本文中に画像を挿入・差し替えできるようにする（自由度の高い操作）

### ラウンドC: 「ジャガーを知る」の内容を1990〜2015年に広げる

- [x] C-1 現状の偏り（初期モデル中心）を確認し、章構成を設計し直す
- [x] C-2 XJ / XK / Xタイプ の年代を軸にした構成へ組み替える
- [x] C-3 英国車としての上質さ・オーナー像・選ばれ方が伝わる導入を書く
- [x] C-4 エンジンの設計思想・鋳造ブロックの堅牢さなど安心材料を反映する
- [ ] C-5 事実関係は CONTENT_FACTCHECK.md の方針に従う（新しい事実主張はDBの検証済み本文から）

### ラウンドD: 仕上げ

- [ ] D-1 型・Lint・テスト・ビルドの通し確認
- [ ] D-2 監査結果をレビュー依頼書に反映
- [ ] D-3 PROJECT_STATUS 更新

---

## 監査で挙がった未検証の指摘（2026-08-17）

検証エージェントが利用上限で全滅したため、**以下は「未検証の候補」**。
自分で確かめてから対応すること。パフォーマンス系は実測してから判断する。

- 車両詳細1ページで約20本のクエリが8段の直列ウォーターフォールで走る
- 記事・図鑑・ライブラリの詳細を開くたびに models 全件スキャンと車両検索が走る
- 年表の年代別表示で関連コンテンツ取得がN+1
- `site_settings` を1ページで2〜3回引いている
- `generateMetadata` と本体で同じ取得を二重に呼んでいる
- 公開レイアウトが `cookies()` を読むため全ルートが動的レンダリングに固定された
- 編集モードのCookieが最大12時間残る
- 削除した車両写真がメディア管理の枚数に数えられる
- 編集モード中に記事本文の段落間の余白が消える
- ライブ編集の上部ページ切り替えが、iframe内で移動した後に効かなくなる
- `20260817020000` のSQLは再実行すると trigger already exists で失敗する

---

## 進捗ログ

| 日時 | 項目 | 内容 |
| ---- | ---- | ---- |
| 2026-08-17 13:10 | 準備 | 第2弾の計画書を作成 |
